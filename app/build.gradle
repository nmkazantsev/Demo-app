plugins {
    alias(libs.plugins.android.application)
}

android {
    namespace 'com.example.androiddemo'
    compileSdk 36

    defaultConfig {
        applicationId "com.example.androiddemo"
        minSdk 24
        targetSdk 36
        versionCode 1
        versionName "1.0"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }
    aaptOptions {
        noCompress 'mp3'   // запрещаем сжатие для файлов .mp3
        // если нужно несколько типов: noCompress 'mp3', 'wav', 'ogg'
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_11
        targetCompatibility JavaVersion.VERSION_11
    }
    packaging {
        resources {
            // Вариант 1 (рекомендуемый): Использовать первый найденный экземпляр файла
            pickFirsts.add('fragment_shader.glsl')
            pickFirsts.add('vertex_shader.glsl')

            // ИЛИ Вариант 2: Исключить дубликат из одного из модулей, если он не нужен
            // excludes.add('/fragment_shader.glsl')

            // ИЛИ Вариант 3: Объединить все экземпляры файла (если они должны быть соединены)
            // merges.add('fragment_shader.glsl')
        }
    }
}

dependencies {
    implementation(project(":core"))
    implementation(project(":android"))
    implementation(project(":game"))
    implementation libs.appcompat
    implementation libs.material
    implementation libs.activity
    implementation libs.constraintlayout
    testImplementation libs.junit
    androidTestImplementation libs.ext.junit
    androidTestImplementation libs.espresso.core

 //   implementation 'com.google.android.exoplayer:exoplayer-core:2.19.1'
//    implementation libs.media3.common
 //   implementation libs.media3.exoplayer
}